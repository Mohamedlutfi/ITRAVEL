<div class="body">
<div class="profile-body">
    <div class="form2">
        <h1>Profile</h1>
    <div class="profile-container " >
    <div class="profile-info">
        <h1>My Profile</h1>
        <p>Welcome to your profile page! {{#if user.fullname}}{{user.fullname}}{{else}}{{session.user}}{{/if}}</p>
        <p>Here you can upload, modify, and delete your travel Memories</p>
        <p>Feel free to explore all the features available to you. Show your memories to friends and family, and keep track of your trips.</p>
        <p>Don't forget to check out the gallery section where you can view all your uploaded memories and all other users' memories.</p>
        <p>If you need any assistance, please reach out to our support team. We're here to help!</p>
        <div class="infobtn">
                <a href="/contact"><button class="btni" role="button"><span class="text">Contact us</span><span> &#8594;</span></button></a>
        </div>
    </div>
    <div class="profile-content">
    <div class="profile-form">
    <form action="/trips" method="post" enctype="multipart/form-data">
        <h2>Create a new Memory</h2>
        <input type="text" name="title" class="t-Input"  placeholder="Memory Title" required>
        <textarea name="description" class="t-Input" maxlength="75" placeholder="Short Memory description" required></textarea>
        <br>
        <input id="trip-file" type="file" name="file" accept="image/*" placeholder="Choose a photo">
        <div id="file-preview" aria-hidden="true" style="display:none; margin-top:8px;">
            <img id="file-preview-img" src="" alt="preview" style="max-width:120px; max-height:120px; border-radius:8px;">
        </div>
        <div id="file-validation" role="status" aria-live="polite" style="margin-top:8px;color:#ffd;display:none;"></div>
        <br>
        <br>
        <button type="submit">Create/Update memory</button>
    </form>
    </div>
    </div>

    </div>
        <h1>My Memories</h1>
    <div class="tripcards profile-trips">
    {{#each trips}}
    <div class="card">
        <img src="{{img}}" alt="{{title}}" class="cardimg">
        <div class="cardoverlay">
            <h2 class="cardtitleoverlay">{{title}}</h2>
            <p class="cardtextoverlay">{{description}}</p>
            <div class="cardoverlaybg"></div>
            <div class="card-actions">
                <button data-id="{{id}}" class="editBtn">Edit</button>
                <button data-id="{{id}}" class="delBtn">Delete</button>
            </div>
        </div>
    </div>
    {{/each}}
    </div>
</div>
</div>
<script src="/js/profile.js"></script>
</div>