<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>ITravel</title>
        <link rel="icon" type="image/x-icon" href="img/newlogo.png">
        <link rel="stylesheet" href="/css/jl-styles.css">
        <link rel="stylesheet" href="/css/Login-style.css">
        <link rel="stylesheet" href="/css/nav.css">
        <link rel="stylesheet" href="/css/home.css">
        <link rel="stylesheet" href="/css/profile.css">
        <link rel="stylesheet" href="/css/about.css">
        <link rel="stylesheet" href="/css/gallary.css">
    </head>
    <body>
 <header class="nav">
            <a href="/home" class="logo"><img src="img/newbrun.png" alt="" width=""><span class="logoTxt">ITravel</span></a>
                <!-- hamburger menu for small screens -->
                <button class="menu-toggle" aria-label="Toggle navigation" aria-expanded="false">
                        <span class="bar bar1"></span>
                        <span class="bar bar2"></span>
                        <span class="bar bar3"></span>
                </button>
        <div class="nav-links">
      
        <a href="http://localhost:8080/home" class="Home link">Home</a>
        <a href="http://localhost:8080/gallary" class="Gallary link">Gallary</a>
        {{#if session.isLoggedIn}}
            {{#if (or (eq session.user "admin") session.userData.isAdmin)}}
                <a href="/admin" class="Admin link">Admin</a>
            {{else}}
                <a href="/MyProfile" class="Profile link">Profile</a>
            {{/if}}
        {{/if}}
                <a href="http://localhost:8080/about" class="About link">About</a>
             
        <a href="http://localhost:8080/contact" class="Contact link">Contact</a>
     </div>   
         <div class="nav-login">
                {{#if session.isLoggedIn}}
                    <a href="/logout" class="Login">Logout</a>
                {{else}}
                    <a href="/login" class="Login">Login</a>
                {{/if}}
         </div>
    </header>

        {{{body}}}
            <footer class="footer">
        <p class="footertext"> &copy; ITravel - 2025</p>
        <p class="footertext">All rights reserved.</p>
        <p class="footertext">Designed by ITravel Team</p>
    </footer>
        <script src="/js/responsive.js"></script>
    </body>
</html>
